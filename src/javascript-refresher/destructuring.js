console.log('---------------------- destructuring -------------------')
// Easily extract array elements or object properties and store them in variables
// Can pull out single elements or properties and store them in variables for both arrays and objects

const myArray = ['James', 'Male'];
[name, gender] = myArray;

console.log(name);
console.log(gender);